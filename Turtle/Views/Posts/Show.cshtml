@model Turtle.Models.Post

@if(ViewBag.Message != null){

    <h2 class="p-3 rounded-3 text-center mb-5 alert @ViewBag.Alert">@ViewBag.Message</h2>
}

<div class="card">
    
    <partial name="PostInfo" model="@Model"></partial>
    
</div>

@* 
    De aici adaugma toate comentariile in mod recursiv
*@
